{
  "mcpServers": {
    "executor": {
      "command": "executor",
      "args": ["mcp"]
    },
    "github": {
      "command": "npx",
      "args": [
        "-y",
        "mcp-remote",
        "https://api.githubcopilot.com/mcp/",
        "--header",
        "Authorization: Bearer ${GITHUB_PERSONAL_ACCESS_TOKEN}"
      ]
    }
  }
}
